{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Execute GMBot tests",
      "type": "node",
      "request": "launch",
      "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
      "args": ["-r", "ts-node/register", "src/**/*.spec.ts", "--exit"],
      "cwd": "${workspaceRoot}"
    }
  ]
}
